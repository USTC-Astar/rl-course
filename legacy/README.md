# legacy：已归档的旧版文件

这里是项目早期版本的遗留文件，已被 VitePress 版课程站完全取代，仅作存档，不再维护。

| 文件 | 说明 |
| --- | --- |
| `index.html` | 旧版单页课程应用（7 课合并单页） |
| `app.js` | 旧版交互逻辑（训练曲线回放、网格动画） |
| `styles.css` | 旧版样式 |

现行课程站位于 `web/docs/`，启动方式见项目根目录 `README.md`：

```bash
# 在项目根目录执行
cd web
npm run dev
```

确认不再需要后，可整体删除本目录。
