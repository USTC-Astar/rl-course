# 贡献指南

感谢你愿意改进这套课程！无论是一处错别字、一个公式排版问题、一道更好的自测题，还是一整节新课，都欢迎。

## 快速上手（5 分钟）

```bash
# 在项目根目录执行
./scripts/bootstrap.sh        # 安装隔离的 Python 环境
cd web && npm install         # 安装网页依赖
npm run dev                   # 打开 http://127.0.0.1:5173 边改边看
```

提交前必须全部通过：

```bash
# 在项目根目录执行
./scripts/run_python.sh -m pytest    # 45 个测试
cd web && npm run build              # 网站构建
```

## 三类常见贡献

### 1. 内容勘误（最欢迎）

改正文、公式、图解、测验题中的错误：

1. 直接修改 `web/docs/lessonXX/index.md`；
2. 若改动影响自测题，同步更新 `web/docs/.vitepress/theme/quiz-bank.ts`；
3. 本地跑一遍 `npm run dev` 确认渲染正常（尤其检查公式和图解）。

### 2. 新增 / 改进实验数据

课程坚持“**真实数据**”原则——正文里的每个数值都必须来自脚本实际运行：

1. 核心算法放进 `src/rl_learning_lab/`，配套 `scripts/generate_*.py` 生成 `web/data/*.json`；
2. **固定随机种子**，把脚本真实打印的预期输出写进课程正文（读者复现结果必须一致）；
3. 在 `tests/` 里为关键计算加测试；若是“注入 bug 的失败案例”，测试要锁定“症状确实发生”；
4. 对照实验双方口径必须一致（同环境、同种子、同策略），真值来自同一对象的精确计算；
5. 把新脚本登记进 `scripts/train_all.sh`。

### 3. 新增课程

新课写作请遵循 `web/docs/` 现有 22 课的统一骨架：

- 每课一条主线：学习目标 → 入门图解 → 问题→原理→例子→验证→局限 → 可运行代码 → 真实曲线 → 常见误区 → 练习 → 5 道自测 → 小结；
- 新概念首次出现：通俗解释 + 中文全称（English Full Name, ABBR）；
- 公式：MathJax 渲染（`$...$` / `$$...$$`）+ 逐符号解释 + 至少一处数字代入。**注意**：把公式放进带样式的 HTML 容器时，容器标签与内容之间必须留空行，否则公式不会渲染；
- 至少一张入门图解或一个交互实验台；SVG 自带本图唯一的 `defs`/marker id；
- `ChapterQuiz` 题库存放在 `quiz-bank.ts`，每课 5 题，正确答案位置打散（新增后统计复查分布）；
- 新符号和术语登记进 `web/docs/reference/glossary.md`；
- 在 `config.mts` 侧边栏、首页 `index.md` 和 `README.md` 课程目录中登记新课。

## 提交 PR 前

- [ ] `./scripts/run_python.sh -m pytest` 全部通过；
- [ ] `cd web && npm run build` 成功；
- [ ] 构建产物中抽查：公式已渲染（非源码）、每个图解用法已渲染（防 kind 拼错）、无本机绝对路径；
- [ ] 涉及随机数据的改动已固定种子并更新正文预期输出；
- [ ] 新增自测题后答案分布仍大致均匀。

## Issue 约定

- 内容错误请用「勘误」模板，注明课程编号和小节；
- 新课建议请用「新课提案」模板，说明它填补哪个缺口、预期前置课程。

## 行为准则

保持友善和具体：讨论算法与教学设计时对事不对人；提问时附上可复现的命令与输出。
